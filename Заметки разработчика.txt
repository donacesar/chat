------------------  Заметки для CHAT ---------------------

> За один раз пересылается максимум 1024 байта. Из них 980 байт => символы сообщения и имени пользователя. (русскими буквами раза в 2 поменьше). Остальные 44 байта на формат(json) и протокол(websocket).